language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-koreq' ]; then cd .. && eval "mv $currentfolder generator-koreq" && cd generator-koreq; fi

